  # name: Deploy All

  # on:
  #   workflow_run:
  #     workflows: ['Client CI', 'Server CI']
  #     types:
  #       - completed
  #     branches: [main]

  # jobs:
  #   deploy-all:
  #     runs-on: ubuntu-latest
  #     if: ${{ github.event.workflow_run.conclusion == 'success' }}
  #     steps:
  #       - name: Deploy All Services
  #         run: |
  #           echo "Deploying all services..."
  #           # Add your combined deployment steps here